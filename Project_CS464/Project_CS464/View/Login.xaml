<Window x:Class="Project_CS464.View.Login"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:Project_CS464"
        xmlns:vm="clr-namespace:Project_CS464.ViewModel"
        mc:Ignorable="d"
        Name="loginWindow" 
        Title="Login" Height="400" Width="500">

    <Window.DataContext>
        <vm:UserViewModel/>
    </Window.DataContext>

    <StackPanel VerticalAlignment="Center" Margin="50">
        <TextBlock Text="Đăng nhập" Margin="20" FontSize="24" FontWeight="Bold" HorizontalAlignment="Center"/>
        <Grid>
            <Grid.RowDefinitions>
                <RowDefinition Height="*"/>
                <RowDefinition Height="*"/>
            </Grid.RowDefinitions>

            <Grid Grid.Row="0" Margin="10">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="2*"/>
                </Grid.ColumnDefinitions>
                <TextBlock Text="Tên đăng nhập" Grid.Column="0"/>
                <TextBox Grid.Column="1" Text="{Binding Username, UpdateSourceTrigger=PropertyChanged}"/>
            </Grid>

            <Grid Grid.Row="1" Margin="10">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="2*"/>
                </Grid.ColumnDefinitions>
                <TextBlock Text="Mật khẩu" Grid.Column="0"/>
                <PasswordBox x:Name="PasswordBoxInput" Grid.Column="1"/>
            </Grid>
        </Grid>

        <Button Content="Đăng nhập" 
                Margin="100, 15, 100, 0" Height="30"
                Command="{Binding LoginCommand}"
                CommandParameter="{Binding ElementName=loginWindow}"/>
    </StackPanel>
</Window>