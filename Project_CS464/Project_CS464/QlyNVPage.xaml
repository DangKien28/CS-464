<Page x:Class="Project_CS464.QLyNVPage"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
      xmlns:local="clr-namespace:Project_CS464"
      mc:Ignorable="d" 
      d:DesignHeight="600" d:DesignWidth="950"
      Title="Quản lý nhân viên"
      Background="#F5F5F5">

    <Page.Resources>
        <Style x:Key="ModernButton" TargetType="Button">
            <Setter Property="Background" Value="#4A90E2"/>
            <Setter Property="Foreground" Value="White"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="Padding" Value="15,10"/>
            <Setter Property="Margin" Value="5"/>
            <Setter Property="FontSize" Value="13"/>
            <Setter Property="FontWeight" Value="SemiBold"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border Background="{TemplateBinding Background}" 
                                CornerRadius="5"
                                Padding="{TemplateBinding Padding}">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                        </Border>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
            <Style.Triggers>
                <Trigger Property="IsMouseOver" Value="True">
                    <Setter Property="Background" Value="#357ABD"/>
                </Trigger>
            </Style.Triggers>
        </Style>

        <Style x:Key="ModernTextBox" TargetType="TextBox">
            <Setter Property="Padding" Value="8,6"/>
            <Setter Property="BorderBrush" Value="#D0D0D0"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="FontSize" Value="13"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="TextBox">
                        <Border Background="{TemplateBinding Background}" 
                                BorderBrush="{TemplateBinding BorderBrush}"
                                BorderThickness="{TemplateBinding BorderThickness}"
                                CornerRadius="4">
                            <ScrollViewer x:Name="PART_ContentHost" Margin="2"/>
                        </Border>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <Style x:Key="ModernGroupBox" TargetType="GroupBox">
            <Setter Property="Background" Value="White"/>
            <Setter Property="BorderBrush" Value="#E0E0E0"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="Padding" Value="10"/>
            <Setter Property="FontWeight" Value="SemiBold"/>
            <Setter Property="FontSize" Value="14"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="GroupBox">
                        <Grid>
                            <Border Background="{TemplateBinding Background}"
                                    BorderBrush="{TemplateBinding BorderBrush}"
                                    BorderThickness="{TemplateBinding BorderThickness}"
                                    CornerRadius="8">
                                <Border.Effect>
                                    <DropShadowEffect BlurRadius="10" ShadowDepth="2" Opacity="0.2"/>
                                </Border.Effect>
                                <Grid>
                                    <Grid.RowDefinitions>
                                        <RowDefinition Height="Auto"/>
                                        <RowDefinition Height="*"/>
                                    </Grid.RowDefinitions>
                                    <Border Background="#4A90E2" 
                                            CornerRadius="8,8,0,0" 
                                            Padding="12,8">
                                        <ContentPresenter ContentSource="Header" 
                                                          TextElement.Foreground="White"
                                                          TextElement.FontWeight="SemiBold"/>
                                    </Border>
                                    <ContentPresenter Grid.Row="1" Margin="{TemplateBinding Padding}"/>
                                </Grid>
                            </Border>
                        </Grid>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
    </Page.Resources>

    <DockPanel>
        <StatusBar DockPanel.Dock="Bottom" Background="#34495E" Foreground="White" Height="30">
            <StatusBarItem>
                <StackPanel Orientation="Horizontal">
                </StackPanel>
            </StatusBarItem>
        </StatusBar>

        <Grid Margin="15">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="250"/>
                <ColumnDefinition Width="*"/>
            </Grid.ColumnDefinitions>

            <GroupBox Header="Thông tin nhân viên" Style="{StaticResource ModernGroupBox}" Grid.ColumnSpan="2" HorizontalAlignment="Right" Width="920">
                <Grid Margin="10">
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="*"/>
                    </Grid.RowDefinitions>
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="140"/>
                        <ColumnDefinition Width="*"/>
                    </Grid.ColumnDefinitions>

                    <Label Grid.Row="0" Grid.Column="0" Content="Mã nhân viên:" Margin="5" FontWeight="SemiBold" Foreground="#555"/>
                    <TextBox x:Name="txt_MaNV" Grid.Row="0" Grid.Column="1" Margin="5" Style="{StaticResource ModernTextBox}"/>

                    <Label Grid.Row="1" Grid.Column="0" Content="Họ tên:" Margin="5" FontWeight="SemiBold" Foreground="#555"/>
                    <TextBox x:Name="txt_HoTen" Grid.Row="1" Grid.Column="1" Margin="5" Style="{StaticResource ModernTextBox}"/>

                    <Label Grid.Row="2" Grid.Column="0" Content="Địa chỉ:" Margin="5" FontWeight="SemiBold" Foreground="#555"/>
                    <TextBox x:Name="txt_DiaChi" Grid.Row="2" Grid.Column="1" Margin="5,5,5,28" 
                             TextWrapping="Wrap" AcceptsReturn="True" VerticalScrollBarVisibility="Auto"
                             Style="{StaticResource ModernTextBox}"/>

                    <Label Grid.Row="2" Grid.Column="0" Content="Giới tính:" Margin="5,50,5,28" FontWeight="SemiBold" Foreground="#555" Grid.RowSpan="2"/>
                    <StackPanel Grid.Row="2" Grid.Column="1" Orientation="Horizontal" Margin="5,57,5,26" Grid.RowSpan="2">
                        <RadioButton x:Name="rdb_nam" Content="Nam" Margin="5,0,15,0" FontSize="13"/>
                        <RadioButton x:Name="rdb_nu" Content="Nữ" Margin="5,0,15,0" FontSize="13"/>
                        <RadioButton x:Name="rdb_khac" Content="Khác" Margin="5,0,15,0" FontSize="13"/>
                    </StackPanel>

                    <Label Content="Ngày sinh:" Grid.Row="3" Grid.Column="0" Margin="5,13,5,31" FontWeight="SemiBold" Foreground="#555" Grid.RowSpan="2"/>
                    <DatePicker x:Name="dp_NgaySinh" Grid.Row="3" Grid.Column="1" Margin="5,13,5,10" Height="32"/>

                    <Grid Grid.Row="5" Grid.Column="0" Grid.ColumnSpan="2" Margin="5,10,5,5">
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="*"/>
                            <ColumnDefinition Width="*"/>
                            <ColumnDefinition Width="*"/>
                            <ColumnDefinition Width="*"/>
                        </Grid.ColumnDefinitions>
                        <Grid.RowDefinitions>
                            <RowDefinition Height="Auto"/>
                            <RowDefinition Height="Auto"/>
                        </Grid.RowDefinitions>

                        <Button Grid.Row="0" Grid.Column="0" Click="Button_Load" Content="Load dữ liệu" Style="{StaticResource ModernButton}" Margin="2,-40,38,44"/>
                        <Button Grid.Row="0" Click="Button_Them" Content="Thêm" Style="{StaticResource ModernButton}" Margin="131,-40,79,44" Grid.ColumnSpan="2"/>
                        <Button Grid.Row="0" Grid.Column="1" Click="Button_Sua" Content="Sửa" Style="{StaticResource ModernButton}" Margin="91,-40,109,44" Grid.ColumnSpan="2"/>
                        <Button Grid.Row="0" Grid.Column="2" Click="Button_xoa" Content="Xóa" Style="{StaticResource ModernButton}" Margin="57,-40,149,44" Grid.ColumnSpan="2"/>

                        <Button Grid.Column="3" Click="Button_Tim" Content="Tìm kiếm" 
                                Style="{StaticResource ModernButton}" Margin="20,-40,14,44"/>
                    </Grid>

                    <DataGrid AutoGenerateColumns="False" 
                              SelectionChanged="DG_NhanVien_SelectionChanged" 
                              x:Name="DG_NhanVien" 
                              Grid.Row="5" 
                              Grid.Column="0" 
                              Grid.ColumnSpan="2" 
                              Margin="5,28,5,0"
                              Background="White"
                              RowBackground="White"
                              AlternatingRowBackground="#F9F9F9"
                              BorderBrush="#D0D0D0"
                              BorderThickness="1"
                              GridLinesVisibility="Horizontal"
                              HorizontalGridLinesBrush="#E8E8E8"
                              HeadersVisibility="Column"
                              CanUserAddRows="False"
                              CanUserDeleteRows="False"
                              IsReadOnly="True"
                              SelectionMode="Single"
                              FontSize="13" Grid.RowSpan="2">
                        <DataGrid.ColumnHeaderStyle>
                            <Style TargetType="DataGridColumnHeader">
                                <Setter Property="Background" Value="#ECF0F1"/>
                                <Setter Property="Foreground" Value="#2C3E50"/>
                                <Setter Property="FontWeight" Value="SemiBold"/>
                                <Setter Property="Padding" Value="10,8"/>
                                <Setter Property="BorderBrush" Value="#BDC3C7"/>
                                <Setter Property="BorderThickness" Value="0,0,1,1"/>
                                <Setter Property="HorizontalContentAlignment" Value="Left"/>
                            </Style>
                        </DataGrid.ColumnHeaderStyle>
                        <DataGrid.CellStyle>
                            <Style TargetType="DataGridCell">
                                <Setter Property="Padding" Value="10,5"/>
                                <Setter Property="BorderThickness" Value="0"/>
                                <Style.Triggers>
                                    <Trigger Property="IsSelected" Value="True">
                                        <Setter Property="Background" Value="#D6EAF8"/>
                                        <Setter Property="Foreground" Value="#000"/>
                                    </Trigger>
                                </Style.Triggers>
                            </Style>
                        </DataGrid.CellStyle>
                        <DataGrid.Columns>
                            <DataGridTextColumn Header="Mã NV" Binding="{Binding MaNV}" Width="100"/>
                            <DataGridTextColumn Header="Họ tên" Binding="{Binding HoTen}" Width="*" MinWidth="150"/>
                            <DataGridTextColumn Header="Địa chỉ" Binding="{Binding DiaChi}" Width="*" MinWidth="180"/>
                            <DataGridTextColumn Header="Giới tính" Binding="{Binding GioiTinh}" Width="90"/>
                            <DataGridTextColumn Header="Ngày sinh" Binding="{Binding NgaySinh, StringFormat=dd/MM/yyyy}" Width="110"/>
                        </DataGrid.Columns>
                    </DataGrid>
                </Grid>
            </GroupBox>
        </Grid>
    </DockPanel>
</Page>